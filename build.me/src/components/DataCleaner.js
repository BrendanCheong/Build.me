
export const DataCleaner = async (info, partName) => {

    return 1
    // var chosenArray = []
    // for(let i = 0 ; i < Object.keys(await info).length; ++i) {
    //     const itemName = info[i].itemName
    //     const itemPrice = info[i].itemPrice
    //     if (itemName.includes(` ${partName} `) && itemPrice !== "NA") {
    //         chosenArray.push(info[i])
    //     }
    // }
    // return chosenArray
}